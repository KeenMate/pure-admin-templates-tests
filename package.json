{
  "name": "pure-admin-test-apps",
  "private": true,
  "description": "E2E harness for apps generated by @keenmate/pureadmin. Not published.",
  "scripts": {
    "playwright:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "playwright": "^1.49.0"
  }
}
